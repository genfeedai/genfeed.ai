# 2026-07-06

## Session 1 — Merge train + DI import-type root-cause + rebase fan-out

### System flow

```text
gh pr list (45 open) ──► overlap matrix (files per PR)
        │
        ├─► wave 1: zero-overlap CLEAN PRs ──► squash-merge (6)
        ├─► wave 2: foundational-first     ──► squash-merge (12)
        ├─► dup pair #1365 vs #1370 ──► diff review ──► merge #1370 (filter-preserving), #1365 to close
        └─► fallout: 10 PRs CONFLICTING ──► parallel rebase agents (merge master into branch, regen openapi, push)

local dirty brands.controller.ts (`import type` on DI services)
        └─► root cause: emitDecoratorMetadata erases type-only imports from design:paramtypes
                └─► scripts/check-di-value-imports.ts guard + CI wiring + memory rule
                        └─► guard found 2 shipped bugs: Skills/MoodBoards @Body DTOs unvalidated
```

### Affected components

- GitHub PRs (genfeedai/genfeed.ai) — 20 squash merges to master
- `scripts/check-di-value-imports.ts` (new AST guard), `package.json`, `.github/workflows/ci.yml` (guards job)
- `apps/server/api/src/collections/skills/controllers/skills.controller.ts`, `mood-boards.controller.ts` (DTO value-import fixes)
- `.agents/memory/rules/nestjs_value_imports_for_di.md` (+ MEMORY.md index)

### What was done

- [x] Reviewed 45 open PRs; built pairwise changed-file overlap matrix; cross-checked 4 running local sessions' branches
- [x] Merged 20 green PRs in dependency order (REST audit #1354, monochrome #1335, pricing, #1090 libs, MCP, brands)
- [x] Reconciled duplicate pair: #1370 merged (preserves per-route default WHERE filters + cache semantics), #1365 superseded — close manually (permission classifier blocked `gh pr close`)
- [x] Root-caused recurring `import type` breakage: server compiles with `emitDecoratorMetadata` (tsconfig.server.decorators.json); type-only imports erase classes from `design:paramtypes` → DI injects undefined / ValidationPipe silently skips. Config is correct; gap was zero enforcement
- [x] Built `check:di-value-imports` guard (constructor DI + `@Body`/`@Query`/`@Param` `*Dto` params; skips decorated ctor params and non-Dto types); wired into CI guards job
- [x] Guard surfaced 2 shipped bugs on master: `SkillsController` (4 DTOs) + `MoodBoardsController` (1 DTO) had unvalidated `@Body` params — fixed to value imports; both spec files pass (11/11)
- [x] Launched parallel rebase agents for conflicting PRs: #1369 ✅ pushed, #1360/#1330/#1328/#1341 in flight; #1308/#1315/#1326/#1333/#1337 queued

### Key decisions

- **Merge order = overlap-driven, foundational first** (#1362 dead-route deletes before other REST PRs) to minimize conflict fallout.
- **#1370 over #1365**: #1365 migrated consumers to bare `?limit=N`, silently dropping the `/latest` routes' default WHERE filters (training exclusion, `isDefault` OR-branch, user scope); #1370 preserved them via `latest=true` shorthand.
- **Guard heuristics**: any ctor-param decorator (`@Inject*`, `@Optional`) ⇒ skip (explicit token/opt-out); method params flagged only for `Body|Query|Param` + `*Dto` suffix (interfaces have no value to import). Naive version produced 1589 false positives.
- **Held #1338** (brands controller import cleanup) — active #1364-fix session edits the same file.

### Mistakes and fixes

- First guard iteration flagged 1589 sites (interfaces, `@Res() Response`, `@InjectQueue` queues) — tightened to 5 true positives.
- Attempted `gh pr close 1365` — denied by permission classifier (PR not created this session); left for Vincent.

### Next steps

- [ ] Close #1365 as superseded by #1370 (manual)
- [ ] Merge rebased PRs as CI greens; re-rebase remaining siblings after each (openapi.json regen)
- [ ] UNSTABLE PRs need CI fixes: #1311, #1313, #1325, #1329, #1339, #1349, #1350, #1352, #1359, #1368
- [ ] Possible duplicate family to reconcile: #1326 vs draft #1312 (MCP tools from OpenAPI); #1337 vs #1329 (unlimited seats)
