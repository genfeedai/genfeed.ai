# 2026-07-06

## Session 1 — Merge train + DI import-type root-cause + rebase fan-out

### System flow

```text
gh pr list (45 open) ──► overlap matrix (files per PR)
        │
        ├─► wave 1: zero-overlap CLEAN PRs ──► squash-merge (6)
        ├─► wave 2: foundational-first     ──► squash-merge (12)
        ├─► dup pair #1365 vs #1370 ──► diff review ──► merge #1370 (filter-preserving), #1365 to close
        └─► fallout: 10 PRs CONFLICTING ──► parallel rebase agents (merge master into branch, regen openapi, push)

local dirty brands.controller.ts (`import type` on DI services)
        └─► root cause: emitDecoratorMetadata erases type-only imports from design:paramtypes
                └─► scripts/check-di-value-imports.ts guard + CI wiring + memory rule
                        └─► guard found 2 shipped bugs: Skills/MoodBoards @Body DTOs unvalidated
```

### Affected components

- GitHub PRs (genfeedai/genfeed.ai) — 20 squash merges to master
- `scripts/check-di-value-imports.ts` (new AST guard), `package.json`, `.github/workflows/ci.yml` (guards job)
- `apps/server/api/src/collections/skills/controllers/skills.controller.ts`, `mood-boards.controller.ts` (DTO value-import fixes)
- `.agents/memory/rules/nestjs_value_imports_for_di.md` (+ MEMORY.md index)

### What was done

- [x] Reviewed 45 open PRs; built pairwise changed-file overlap matrix; cross-checked 4 running local sessions' branches
- [x] Merged 20 green PRs in dependency order (REST audit #1354, monochrome #1335, pricing, #1090 libs, MCP, brands)
- [x] Reconciled duplicate pair: #1370 merged (preserves per-route default WHERE filters + cache semantics), #1365 superseded — close manually (permission classifier blocked `gh pr close`)
- [x] Root-caused recurring `import type` breakage: server compiles with `emitDecoratorMetadata` (tsconfig.server.decorators.json); type-only imports erase classes from `design:paramtypes` → DI injects undefined / ValidationPipe silently skips. Config is correct; gap was zero enforcement
- [x] Built `check:di-value-imports` guard (constructor DI + `@Body`/`@Query`/`@Param` `*Dto` params; skips decorated ctor params and non-Dto types); wired into CI guards job
- [x] Guard surfaced 2 shipped bugs on master: `SkillsController` (4 DTOs) + `MoodBoardsController` (1 DTO) had unvalidated `@Body` params — fixed to value imports; both spec files pass (11/11)
- [x] Launched parallel rebase agents for conflicting PRs: #1369 ✅ pushed, #1360/#1330/#1328/#1341 in flight; #1308/#1315/#1326/#1333/#1337 queued

### Key decisions

- **Merge order = overlap-driven, foundational first** (#1362 dead-route deletes before other REST PRs) to minimize conflict fallout.
- **#1370 over #1365**: #1365 migrated consumers to bare `?limit=N`, silently dropping the `/latest` routes' default WHERE filters (training exclusion, `isDefault` OR-branch, user scope); #1370 preserved them via `latest=true` shorthand.
- **Guard heuristics**: any ctor-param decorator (`@Inject*`, `@Optional`) ⇒ skip (explicit token/opt-out); method params flagged only for `Body|Query|Param` + `*Dto` suffix (interfaces have no value to import). Naive version produced 1589 false positives.
- **Held #1338** (brands controller import cleanup) — active #1364-fix session edits the same file.

### Mistakes and fixes

- First guard iteration flagged 1589 sites (interfaces, `@Res() Response`, `@InjectQueue` queues) — tightened to 5 true positives.
- Attempted `gh pr close 1365` — denied by permission classifier (PR not created this session); left for Vincent.

### Next steps

- [ ] Close #1365 as superseded by #1370 (manual)
- [ ] Merge rebased PRs as CI greens; re-rebase remaining siblings after each (openapi.json regen)
- [ ] UNSTABLE PRs need CI fixes: #1311, #1313, #1325, #1329, #1339, #1349, #1350, #1352, #1359, #1368
- [ ] Possible duplicate family to reconcile: #1326 vs draft #1312 (MCP tools from OpenAPI); #1337 vs #1329 (unlimited seats)

## Session 2 — Fable prep-pass: roadmap contracts + deploy firefighting + SSM region gotcha

### Flow

```
roadmap ask → gh board audit → 5 parallel Explore audits (scheduler/mongoId/nodes/modes/auth)
  → contracts & runbooks posted to issues → board hygiene
deploy dispatched → startup_failure (workflow perms) → #1412 (codex) merged
  → QA gate fail (MCP artifact drift) → PR #1414 (regen) merged → deploy LANDED
v0.5.0 publish fail ×2 (same drift + docker tsc chain) → PR #1417 (pricing tsconfig ref) green, unmerged
webhook 401 check → 500 → SSM region mismatch found (params in us-east-1, prod reads us-west-1)
```

### What was done

- [x] **Contracts/PRDs posted** (all as dated comments, supersede bodies): scheduler domain contract on #1123 (PostGroup model, `post-publish` queue w/ jobId=postId dedupe-lock, two-level state machines, D1–D8) + resolved open questions on #1126/#1127/#1128/#1129; auth Phase-4 cutover runbook on #739; mongoId 5-phase retirement plan on #1041 (incl. NEW finding: 4 gates require 24-hex and reject cuids — live bugs); deployment-mode locked API on #742 (13 mechanisms/115 files measured); implement-or-delete verdicts on #481 (21 of 34 SaaS node types dead; EXECUTOR_REGISTRY is dead code; SaaS videoInput shadows core); execution-order + conflict matrix on #1090/#1343.
- [x] **Board hygiene**: #707 closed (already done — 109-line dispatcher + handlers exist); #334 rescoped to prod E2E walkthrough; #738 retitled web-only, mobile split → #1409 (deferred); #1411 filed (NO password-reset flow exists — Sell-Ready blocker); #1419 filed (docs prerender breaks deploy job); 11 issues milestoned; 8 stale In Progress → Backlog on Project #12.
- [x] **Deploy firefighting**: PR #1414 (regen MCP tools artifact — drift from dep update #1403 failed full-suite QA gate) merged, deploy landed (ECS+app+website+smoke green, docs Vercel failed → #1419). PR #1417 (pricing tsconfig: missing `@genfeedai/interfaces` project reference broke self-hosted docker tsc chain) green, **unmerged**.
- [x] **SSM region mismatch diagnosed**: webhook secrets written to us-east-1 (CLI default); prod reads us-west-1 only. TF `aws_ssm_parameters_by_path` auto-injects — no enumeration needed, next deploy picks up once params re-created in us-west-1. STRIPE_PROMOTION_CODE_LAUNCH confirmed present in us-west-1 → launch pricing live.

### Key decisions

- Scheduler: `Post` stays the channel-target unit; one new `PostGroup` model; sweep becomes producer onto new `post-publish` queue (processor in workers). No second publish engine.
- Workflow refactor order locked: #1344→#1345→#1346→#1347 sequential; #1151 → #481 → #521 → #1343 last (delete-then-move).
- #742: functions not constants, one `envFlag()` truthiness, hostname detection deleted, `GenfeedMode` retired.
- Failed v0.5.0 tag re-cut same version (never burn) — re-cut AGAIN pending (#1414+#1417 both needed in tag tree).

### Mistakes and fixes

- Gave webhook curl path without `v1` prefix + `/callback` suffix (404s) → corrected.
- Claimed "zero further wiring" for SSM before checking region → 500s exposed the us-east-1/us-west-1 mismatch.
- Misread deploy run pair as double-dispatch; Vincent had cancelled one and dispatched one per instruction.

### Next steps

- [ ] Vincent: SSM region-fix loop (put us-west-1, delete us-east-1 strays) — tonight
- [ ] Vincent: merge #1417; redeploy tonight or let daily deploy carry secrets → curl 401 ×3 → close #474
- [ ] Vincent: EARLYGENFEED checkout → close #1285
- [ ] Re-cut v0.5.0 after #1417 in master (delete release+tag, re-tag, recreate)
- [ ] Next Codex wave: #1127 → #1151, #521 → #481, #742 (#707 REMOVED — already done); #1041 Phase 1; #1411; #1419
- [ ] Human Review queue: #33, #232, #330, #738
