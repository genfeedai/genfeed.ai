---
name: MongoDB Atlas URI
description: Atlas cluster URI for genfeed.ai cloud database — use for direct brand/org queries
type: reference
---

Connection string for the `cloud` database on MongoDB Atlas is stored in your local `.env` or password manager — do not commit it here.

Usage: `mongosh "<URI>" --eval '...' --quiet`
