---
description: Desktop BYOK generation runs in Electron main behind the canonical app UI
type: project
status: active
last_verified: 2026-08-05
topics: [desktop, generation, byok, offline]
---

**Rule:** Genfeed Desktop local/BYOK generation runs in the Electron **main process**,
reached over the preload bridge from the canonical `apps/app` UI the shell serves. It needs
no local NestJS API clone.

**Current split:**
- The desktop shell serves `apps/app` (#2371). There is no desktop-local renderer, so every
  BYOK surface is an `apps/app` surface calling the bridge — never a parallel desktop UI.
- Genfeed Connect sign-in (system-browser PKCE -> `gf_` API key + Better Auth session cookie)
  is what gates cloud features. BYOK provider keys are separate, stored locally by main.
- Replicate and fal.ai BYOK image asset generation runs from Electron main, using the user's
  locally stored provider key.
- Generated BYOK assets are written into the workspace asset directory and registered as
  `local-generation`, `local-only`, `uploadPolicy: never` by default.
- Ollama, LM Studio, and generic OpenAI-compatible presets currently cover text/workflow JSON
  through `/chat/completions`; claim image/video/audio support only when a concrete local
  asset adapter exists.
- The desktop sign-in surface now exposes explicit local mode under #2824. That
  mode starts PGlite and the Electron-main local services on demand. It does not
  imply that every configured provider is network-free: Ollama/LM Studio can be
  local, while Replicate/fal.ai and generic remote endpoints still use the
  network and must be labeled accordingly.

**How to apply:**
- Keep local/BYOK generation in Electron main; expose it to `apps/app` only through the
  bridge methods that have a live main-process handler.
- Keep generated desktop assets local unless the user explicitly syncs.
- Never reintroduce a desktop-local Next.js renderer to host a BYOK screen.
- Treat GitHub issue #364 as the BYOK external-provider asset adapter task and issue #255 as
  the parent desktop local generation epic.
