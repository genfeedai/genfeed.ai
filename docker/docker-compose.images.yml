# Docker Compose for images.genfeed.ai GPU instance
# Runs: NestJS API + ComfyUI + Redis on a g6e.xlarge (L40S 48GB)

services:
  api:
    image: ghcr.io/genfeedai/cloud/server:${IMAGE_TAG:-latest}
    command: ["bun", "--filter", "@genfeedai/images", "start:prod"]
    container_name: images-api
    ports:
      - '8189:3031'
    environment:
      - REDIS_URL=redis://redis:6379
      - COMFYUI_URL=http://comfyui:8188
      - NODE_ENV=production
      - PORT=3031
      - SERVICE_NAME=images
      - VERSION=1.0.0
    env_file:
      - .env.images
    depends_on:
      redis:
        condition: service_healthy
      comfyui:
        condition: service_started
    restart: unless-stopped
    networks:
      - fleet-network
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:3031/health']
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

  comfyui:
    image: ghcr.io/genfeedai/comfyui:${COMFYUI_TAG:-latest}
    container_name: images-comfyui
    ports:
      - '8188:8188'
    volumes:
      - comfyui-models:/opt/ComfyUI/models
      - comfyui-output:/opt/ComfyUI/output
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    networks:
      - fleet-network

  redis:
    image: redis:7-alpine
    container_name: images-redis
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    restart: unless-stopped
    networks:
      - fleet-network
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 30s
      timeout: 3s
      retries: 5

volumes:
  comfyui-models:
  comfyui-output:
  redis-data:

networks:
  fleet-network:
    driver: bridge
