{
  "$schema": "https://v2-9-12.turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": [
        "//#env:check",
        "@genfeedai/auth-client#build",
        "@genfeedai/config#build",
        "@genfeedai/pricing#build"
      ],
      "env": [
        "$TURBO_EXTENDS$",
        "BUILD_ID",
        "VERCEL_GIT_COMMIT_SHA",
        "RELEASE_TAG"
      ],
      "inputs": ["$TURBO_EXTENDS$", "$TURBO_ROOT$/package.json"]
    }
  }
}
