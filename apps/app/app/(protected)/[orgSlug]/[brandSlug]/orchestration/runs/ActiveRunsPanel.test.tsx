import { describe, expect, it } from 'vitest';
import Module from './ActiveRunsPanel.tsx';

describe('ActiveRunsPanel.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
