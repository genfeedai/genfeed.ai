import { describe, expect, it } from 'vitest';
import Module from './agent-strategies-page.tsx';

describe('agent-strategies-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
