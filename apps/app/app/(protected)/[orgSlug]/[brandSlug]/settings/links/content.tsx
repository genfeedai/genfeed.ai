/**
 * @deprecated Links CRUD lives on Brand Profile via ModalBrandLink.
 * The route redirects to /settings — this file is kept only so stale imports
 * fail loudly in review rather than shipping a ghost page.
 */
export default function BrandSettingsLinksPage() {
  return null;
}
