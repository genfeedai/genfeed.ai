import { describe, expect, it } from 'vitest';
import Module from './newsletters-page.tsx';

describe('newsletters-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
