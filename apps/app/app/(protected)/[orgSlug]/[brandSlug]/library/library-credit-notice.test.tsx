import { describe, expect, it } from 'vitest';
import Module from './library-credit-notice.tsx';

describe('library-credit-notice.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
