import { createPageMetadata } from '@helpers/media/metadata/page-metadata.helper';

export const generateMetadata = createPageMetadata('Batch History');

export default function StudioBatchHistoryPage() {
  return null;
}
