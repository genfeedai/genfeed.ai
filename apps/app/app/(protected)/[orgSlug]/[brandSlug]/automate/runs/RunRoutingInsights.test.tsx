import { describe, expect, it } from 'vitest';
import Module from './RunRoutingInsights.tsx';

describe('RunRoutingInsights.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
