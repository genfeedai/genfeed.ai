import { describe, expect, it } from 'vitest';
import Module from './organization-identity-defaults-card.tsx';

describe('organization-identity-defaults-card.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
