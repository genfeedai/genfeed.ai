import { describe, expect, it } from 'vitest';
import Module from './settings-agents-page.tsx';

describe('settings-agents-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
