import { describe, expect, it } from 'vitest';
import Module from './content.tsx';

describe('settings-organization-policy-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
