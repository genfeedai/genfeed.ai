import { describe, expect, it } from 'vitest';
import Module from './settings-conversation-page';

describe('settings-conversation-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
