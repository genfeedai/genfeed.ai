{
  "dependencies": {
    "@genfeedai/config": "workspace:*"
  },
  "description": "Genfeed Voices Service",
  "devDependencies": {},
  "main": "../dist/apps/voices/main.js",
  "name": "@genfeedai/voices",
  "private": true,
  "scripts": {
    "build": "cd .. && nest build voices",
    "build:prod": "cd .. && nest build voices && cd voices && if [ -n \"$SENTRY_AUTH_TOKEN\" ]; then bun run sentry:sourcemaps || true; fi",
    "deps:update": "bunx npm-check-updates -u --reject typescript && bun install",
    "dev": "cd .. && NODE_OPTIONS=\"--max-old-space-size=2048 --expose-gc\" nest start voices --watch",
    "lint": "biome lint --write src",
    "sentry:inject": "sentry-cli sourcemaps inject --org genfeedai --project voices-genfeed-ai ../dist",
    "sentry:sourcemaps": "bun run sentry:inject && bun run sentry:upload",
    "sentry:upload": "sentry-cli sourcemaps upload --org genfeedai --project voices-genfeed-ai ../dist",
    "start": "cd .. && nest start voices",
    "start:debug": "cd .. && nest start voices --debug --watch",
    "start:dev": "cd .. && NODE_OPTIONS=\"--max-old-space-size=2048 --expose-gc\" nest start voices --watch",
    "start:prod": "node -r tsconfig-paths/register --max-old-space-size=8192 --expose-gc ../dist/apps/voices/main",
    "test": "echo 'No tests yet'",
    "test:cov": "echo 'No tests yet'"
  },
  "version": "1.0.0"
}
