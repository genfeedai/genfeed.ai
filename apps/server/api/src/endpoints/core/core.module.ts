import { Module } from '@nestjs/common';

/**
 * Core endpoints module — self-hosted API surface.
 * Placeholder from Epic #95 (self-hosted + cloud unification).
 */
@Module({})
export class CoreModule {}
