import { TagResolutionModule } from '@api/shared/services/tag-resolution/tag-resolution.module';

describe('TagResolutionModule', () => {
  it('should be defined', () => {
    expect(TagResolutionModule).toBeDefined();
  });
});
