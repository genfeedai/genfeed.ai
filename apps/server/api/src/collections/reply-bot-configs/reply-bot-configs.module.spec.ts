import { ReplyBotConfigsModule } from '@api/collections/reply-bot-configs/reply-bot-configs.module';

describe('ReplyBotConfigsModule', () => {
  it('should be defined', () => {
    expect(ReplyBotConfigsModule).toBeDefined();
  });
});
