import { UserSubscriptionsModule } from '@api/collections/user-subscriptions/user-subscriptions.module';

describe('UserSubscriptionsModule', () => {
  it('should be defined', () => {
    expect(UserSubscriptionsModule).toBeDefined();
  });
});
