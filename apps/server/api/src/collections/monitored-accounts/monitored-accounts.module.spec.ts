import { MonitoredAccountsModule } from '@api/collections/monitored-accounts/monitored-accounts.module';

describe('MonitoredAccountsModule', () => {
  it('should be defined', () => {
    expect(MonitoredAccountsModule).toBeDefined();
  });
});
