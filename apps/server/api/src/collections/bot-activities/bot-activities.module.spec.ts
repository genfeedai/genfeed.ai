import { BotActivitiesModule } from '@api/collections/bot-activities/bot-activities.module';

describe('BotActivitiesModule', () => {
  it('should be defined', () => {
    expect(BotActivitiesModule).toBeDefined();
  });
});
