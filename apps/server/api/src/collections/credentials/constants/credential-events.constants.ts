/**
 * Emitted after a connect/reconnect settles a credential's provider profile.
 * Payload: `CredentialProfileSyncedEvent`.
 */
export const CREDENTIAL_PROFILE_SYNCED_EVENT = 'credential.profile.synced';
