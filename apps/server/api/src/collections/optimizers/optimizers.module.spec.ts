import { OptimizersModule } from '@api/collections/optimizers/optimizers.module';

describe('OptimizersModule', () => {
  it('should be defined', () => {
    expect(OptimizersModule).toBeDefined();
  });
});
