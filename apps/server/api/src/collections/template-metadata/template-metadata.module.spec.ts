import { TemplateMetadataModule } from '@api/collections/template-metadata/template-metadata.module';

describe('TemplateMetadataModule', () => {
  it('should be defined', () => {
    expect(TemplateMetadataModule).toBeDefined();
  });
});
