import { TrackedLinksModule } from '@api/collections/tracked-links/tracked-links.module';

describe('TrackedLinksModule', () => {
  it('should be defined', () => {
    expect(TrackedLinksModule).toBeDefined();
  });
});
