import { ProcessedTweetsModule } from '@api/collections/processed-tweets/processed-tweets.module';

describe('ProcessedTweetsModule', () => {
  it('should be defined', () => {
    expect(ProcessedTweetsModule).toBeDefined();
  });
});
