import { FontFamiliesModule } from '@api/collections/font-families/font-families.module';

describe('FontFamiliesModule', () => {
  it('should be defined', () => {
    expect(FontFamiliesModule).toBeDefined();
  });
});
