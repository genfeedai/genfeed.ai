import { ContentSchedulesModule } from '@api/collections/content-schedules/content-schedules.module';

describe('ContentSchedulesModule', () => {
  it('should be defined', () => {
    expect(ContentSchedulesModule).toBeDefined();
  });
});
