import { ContentScheduleSchema } from '@api/collections/content-schedules/schemas/content-schedule.schema';

describe('ContentScheduleSchema', () => {
  it('should be defined', () => {
    expect(ContentScheduleSchema).toBeDefined();
  });
});
