import { ContentDraftsModule } from '@api/collections/content-drafts/content-drafts.module';

describe('ContentDraftsModule', () => {
  it('should be defined', () => {
    expect(ContentDraftsModule).toBeDefined();
  });
});
