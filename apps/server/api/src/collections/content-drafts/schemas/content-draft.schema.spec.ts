import { ContentDraftSchema } from '@api/collections/content-drafts/schemas/content-draft.schema';

describe('ContentDraftSchema', () => {
  it('should be defined', () => {
    expect(ContentDraftSchema).toBeDefined();
  });
});
