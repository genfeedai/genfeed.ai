import { ElementsLightingsModule } from '@api/collections/elements/lightings/lightings.module';

describe('ElementsLightingsModule', () => {
  it('should be defined', () => {
    expect(ElementsLightingsModule).toBeDefined();
  });
});
