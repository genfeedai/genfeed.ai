import { ElementsScenesModule } from '@api/collections/elements/scenes/scenes.module';

describe('ElementsScenesModule', () => {
  it('should be defined', () => {
    expect(ElementsScenesModule).toBeDefined();
  });
});
