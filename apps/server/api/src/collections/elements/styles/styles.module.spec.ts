import { ElementsStylesModule } from '@api/collections/elements/styles/styles.module';

describe('ElementsStylesModule', () => {
  it('should be defined', () => {
    expect(ElementsStylesModule).toBeDefined();
  });
});
