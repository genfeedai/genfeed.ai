import { ElementsLensesModule } from '@api/collections/elements/lenses/lenses.module';

describe('ElementsLensesModule', () => {
  it('should be defined', () => {
    expect(ElementsLensesModule).toBeDefined();
  });
});
