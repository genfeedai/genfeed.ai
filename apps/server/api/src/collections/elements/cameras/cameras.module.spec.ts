import { ElementsCamerasModule } from '@api/collections/elements/cameras/cameras.module';

describe('ElementsCamerasModule', () => {
  it('should be defined', () => {
    expect(ElementsCamerasModule).toBeDefined();
  });
});
