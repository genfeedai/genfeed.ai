import { ElementsMoodsModule } from '@api/collections/elements/moods/moods.module';

describe('ElementsMoodsModule', () => {
  it('should be defined', () => {
    expect(ElementsMoodsModule).toBeDefined();
  });
});
