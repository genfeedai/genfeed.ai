import { ElementsSoundsModule } from '@api/collections/elements/sounds/sounds.module';

describe('ElementsSoundsModule', () => {
  it('should be defined', () => {
    expect(ElementsSoundsModule).toBeDefined();
  });
});
