import { TrainingsModule } from '@api/collections/trainings/trainings.module';

describe('TrainingsModule', () => {
  it('should be defined', () => {
    expect(TrainingsModule).toBeDefined();
  });
});
