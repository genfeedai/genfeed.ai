import { MediaUrlsModule } from '@api/services/media-urls/media-urls.module';

describe('MediaUrlsModule', () => {
  it('should be defined', () => {
    expect(MediaUrlsModule).toBeDefined();
  });
});
