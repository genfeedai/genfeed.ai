import { VideoCompletionModule } from '@api/services/video-completion/video-completion.module';

describe('VideoCompletionModule', () => {
  it('should be defined', () => {
    expect(VideoCompletionModule).toBeDefined();
  });
});
