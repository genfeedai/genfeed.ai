import { FilesClientModule } from '@api/services/files-microservice/client/files-client.module';

describe('FilesClientModule', () => {
  it('should be defined', () => {
    expect(FilesClientModule).toBeDefined();
  });
});
