import { FileQueueModule } from '@api/services/files-microservice/queue/file-queue.module';

describe('FileQueueModule', () => {
  it('should be defined', () => {
    expect(FileQueueModule).toBeDefined();
  });
});
