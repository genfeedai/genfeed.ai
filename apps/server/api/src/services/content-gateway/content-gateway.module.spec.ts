import { ContentGatewayModule } from '@api/services/content-gateway/content-gateway.module';

describe('ContentGatewayModule', () => {
  it('should be defined', () => {
    expect(ContentGatewayModule).toBeDefined();
  });
});
