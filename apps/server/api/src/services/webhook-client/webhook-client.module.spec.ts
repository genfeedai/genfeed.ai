import { WebhookClientModule } from '@api/services/webhook-client/webhook-client.module';

describe('WebhookClientModule', () => {
  it('should be defined', () => {
    expect(WebhookClientModule).toBeDefined();
  });
});
