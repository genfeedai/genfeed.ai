import { ApiKeyHelperModule } from '@api/services/api-key/api-key-helper.module';

describe('ApiKeyHelperModule', () => {
  it('should be defined', () => {
    expect(ApiKeyHelperModule).toBeDefined();
  });
});
