import { UnipileModule } from '@api/services/integrations/unipile/unipile.module';

describe('UnipileModule', () => {
  it('should be defined', () => {
    expect(UnipileModule).toBeDefined();
  });
});
