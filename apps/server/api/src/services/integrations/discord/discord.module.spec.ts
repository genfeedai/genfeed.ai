import { DiscordModule } from '@api/services/integrations/discord/discord.module';

describe('DiscordModule', () => {
  it('should be defined', () => {
    expect(DiscordModule).toBeDefined();
  });
});
