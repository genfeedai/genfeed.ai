import { ReplicateModule } from '@api/services/integrations/replicate/replicate.module';

describe('ReplicateModule', () => {
  it('should be defined', () => {
    expect(ReplicateModule).toBeDefined();
  });
});
