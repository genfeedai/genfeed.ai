{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Official Replicate API schema for openai/sora-2",
  "properties": {
    "prompt": {
      "type": "string",
      "title": "Prompt",
      "x-order": 1,
      "description": "A text description of the video to generate"
    },
    "seconds": {
      "allOf": [
        {
          "$ref": "#/components/schemas/seconds"
        }
      ],
      "default": 4,
      "x-order": 3,
      "description": "Duration of the video in seconds"
    },
    "aspect_ratio": {
      "allOf": [
        {
          "$ref": "#/components/schemas/aspect_ratio"
        }
      ],
      "default": "portrait",
      "x-order": 4,
      "description": "Aspect ratio of the video. Portrait is 720x1280, landscape is 1280x720"
    },
    "openai_api_key": {
      "type": "string",
      "title": "Openai Api Key",
      "format": "password",
      "x-order": 0,
      "nullable": true,
      "writeOnly": true,
      "description": "Optional: Your OpenAI API key. If you use your own OpenAI API key, you will be charged directly by OpenAI.",
      "x-cog-secret": true
    },
    "input_reference": {
      "type": "string",
      "title": "Input Reference",
      "format": "uri",
      "x-order": 2,
      "nullable": true,
      "description": "An optional image to use as the first frame of the video. The image must be the same aspect ratio as the video."
    }
  },
  "required": ["prompt"],
  "title": "sora-2 Input Schema",
  "type": "object"
}
