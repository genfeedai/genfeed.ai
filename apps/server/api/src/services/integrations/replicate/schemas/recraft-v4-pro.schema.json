{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Official Replicate API schema for recraft-ai/recraft-v4-pro",
  "properties": {
    "size": {
      "allOf": [
        {
          "$ref": "#/components/schemas/size"
        }
      ],
      "default": "2048x2048",
      "x-order": 2,
      "description": "Width and height of the generated image. Size is ignored if an aspect ratio is set."
    },
    "prompt": {
      "type": "string",
      "title": "Prompt",
      "x-order": 0,
      "description": "Text prompt for image generation (up to 10,000 characters)"
    },
    "aspect_ratio": {
      "allOf": [
        {
          "$ref": "#/components/schemas/aspect_ratio"
        }
      ],
      "default": "Not set",
      "x-order": 1,
      "description": "Aspect ratio of the generated image"
    }
  },
  "required": ["prompt"],
  "title": "recraft-v4-pro Input Schema",
  "type": "object"
}
