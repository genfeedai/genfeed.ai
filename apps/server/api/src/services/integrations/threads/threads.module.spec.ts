import { ThreadsModule } from '@api/services/integrations/threads/threads.module';

describe('ThreadsModule', () => {
  it('should be defined', () => {
    expect(ThreadsModule).toBeDefined();
  });
});
