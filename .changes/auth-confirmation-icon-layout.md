packages: props, ui

The email confirmation screen lines the status icon up with its message.

- `props`: auth form layout props accept the confirmation icon beside the copy.
- `ui`: `AuthFormLayout` renders that icon in the message row.
