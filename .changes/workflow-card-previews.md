packages: props

Add shared workflow-card preview props for optional graph nodes, edges, and
selected media. Workflow library and template cards render existing graph data
when no usable cover is selected. Older summaries without graph data remain
supported and display an unavailable preview. No API requests or migrations are added.
