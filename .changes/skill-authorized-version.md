packages: @genfeedai/contracts

Resolved runtime skills carry the authorized `versionId` and `contentHash`.
`ResolveActiveSkillsContext.pinnedSkills` keeps a retry on the version captured
for that execution.
