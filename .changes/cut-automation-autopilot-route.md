packages: @genfeedai/contracts

Remove `APP_ROUTES.AUTOMATION.AUTOPILOT` (`/automation/autopilot`).

That desk is gone. Schedule, budget, and autonomy live on the agent. Do not add a redirect; the path is not part of the product.
