packages: @genfeedai/api @genfeedai/workers

Proactive agents now receive deployment-wide dispatcher coverage without installing a workflow template. Existing tenant installations remain authoritative, including paused and deleted controls. Missing analytics and content-loop workflows receive the same canonical fallback scheduling.

Weekly budgets default to five daily budgets, while explicit zero budgets remain meaningful. Terminal proactive turns atomically record consumed ledger credits and run history once; generated batch drafts retain their strategy attribution.
