packages: @genfeedai/api, @genfeedai/contracts, @genfeedai/hooks

Stop automation overview from loading 50 full workflow graphs to count
scheduled rows. List executions by brand with a 20-row page. Filter agent
run history by strategyId on the server instead of downloading 100 runs.
Share agent-type labels, icons, and default budgets.
