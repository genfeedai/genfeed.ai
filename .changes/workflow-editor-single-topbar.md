packages: @genfeedai/workflows, @genfeedai/app

Workflow editor uses one SectionTopbar. Graph File/undo/rename chrome
sits in that bar instead of a second stacked toolbar. Image Input and
Video Input are back in the cloud palette as engine-native nodes.
