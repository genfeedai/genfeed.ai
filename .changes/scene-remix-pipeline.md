packages: @genfeedai/actions @genfeedai/contracts @genfeedai/pages @genfeedai/serializers @genfeedai/services

Scene remix adds the hidden `brand-remix.scene-step` action, scene quote and execute contracts, and Studio run helpers. Scene stills and avatar clips stay guided-only: strict fidelity is rejected before dispatch, and completed provenance cannot label that output as strict.
