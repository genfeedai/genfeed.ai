packages: @genfeedai/props @genfeedai/pages

`DiscoveryDeskSource` includes `imported` for a single post saved by URL or the browser extension. `toDeskItemFromSourcePost` accepts `isImportedPost` and classifies those posts apart from owned accounts and followed creators. Imported posts are remix sources, not brand Knowledge.
