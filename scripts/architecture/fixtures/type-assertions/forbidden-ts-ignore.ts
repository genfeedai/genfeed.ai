declare const fixtureValue: unknown;

// biome-ignore lint/suspicious/noTsIgnore: deliberate forbidden checker fixture
// @ts-ignore -- the architecture checker must reject even documented instances
export const forbiddenText: string = fixtureValue;
