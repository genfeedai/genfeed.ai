export interface ButtonCreditsProps {
  /**
   * When true, renders the button in a compact layout that matches
   * the dimensions of the app switcher button. Used for menus that
   * do not display the brand switcher and need a smaller footprint.
   */
  isCompact?: boolean;
}
