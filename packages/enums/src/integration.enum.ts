export enum IntegrationPlatform {
  TELEGRAM = 'telegram',
  SLACK = 'slack',
  DISCORD = 'discord',
  UNIPILE = 'unipile',
}

export enum IntegrationStatus {
  ACTIVE = 'active',
  PAUSED = 'paused',
  ERROR = 'error',
}
