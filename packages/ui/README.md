# @genfeedai/ui

Shared UI primitives for cross-product reuse.

This package is intentionally narrow and should only contain platform-safe,
token-driven components that can be used by cloud web, desktop, and extension
surfaces.
