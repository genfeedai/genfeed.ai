import type { Meta, StoryObj } from '@storybook/nextjs';

const meta: Meta = {
  argTypes: {},
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
  title: 'Components/Helpers/Data/Data.helper',
};

export default meta;
type Story = StoryObj;

export const Default: Story = {
  args: {},
};

export const Interactive: Story = {
  args: {},
};
