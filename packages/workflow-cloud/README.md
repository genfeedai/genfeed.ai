# Workflow Cloud

Reserved cloud-specific workflow package directory.

This directory is currently not a formal package. Keep it empty or intentional until cloud-only workflow logic is extracted into a real package boundary.
