# Contexts

Source-only shared React context directory.

This package tree contains reusable context providers and context state modules used across app surfaces. It is currently a source folder, not a standalone published package.
