/**
 * Upper bound for an organization's per-platform daily publishing cap
 * (`OrganizationSetting.quota*`). A cap of 0 means the platform is unmetered.
 */
export const PUBLISHING_QUOTA_MAX = 1000;
