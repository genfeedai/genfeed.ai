import type { IconComponent } from '@genfeedai/contracts/types/icon';

export interface IPlatformBadgeConfig {
  icon: IconComponent;
  label: string;
  bgColor: string;
  textColor: string;
  iconColor: string;
}
