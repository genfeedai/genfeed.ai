import { gifAttributes } from '@serializers/attributes/ingredients/gif.attributes';
import { simpleConfig } from '@serializers/builders';

export const gifSerializerConfig = simpleConfig('gif', gifAttributes);
