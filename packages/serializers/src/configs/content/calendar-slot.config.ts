import { calendarSlotAttributes } from '@serializers/attributes/content/calendar-slot.attributes';

export const calendarSlotSerializerConfig = {
  attributes: calendarSlotAttributes,
  type: 'calendar-slot',
};
