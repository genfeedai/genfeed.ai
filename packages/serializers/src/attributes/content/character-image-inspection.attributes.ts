import { createEntityAttributes } from '@genfeedai/helpers';
export const characterImageInspectionAttributes = createEntityAttributes([
  'hasFace',
  'isCharacter',
  'characterId',
  'handle',
  'label',
]);
