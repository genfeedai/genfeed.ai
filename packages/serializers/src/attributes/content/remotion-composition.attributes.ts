import { createEntityAttributes } from '@genfeedai/helpers';
export const remotionCompositionAttributes = createEntityAttributes([
  'label',
  'version',
  'rendererVersion',
  'inputSchema',
  'outputSettings',
]);
