import { createEntityAttributes } from '@genfeedai/helpers';

export const brandMemoryAttributes = createEntityAttributes([
  'organization',
  'brand',
  'date',
  'entries',
  'insights',
  'metrics',
]);
