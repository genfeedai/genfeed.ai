import { createEntityAttributes } from '@genfeedai/helpers';

export const analyticsEngagementAttributes = createEntityAttributes([
  'likes',
  'comments',
  'shares',
  'saves',
  'total',
  'percentages',
]);
