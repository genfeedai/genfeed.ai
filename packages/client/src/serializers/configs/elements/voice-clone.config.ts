import { voiceCloneAttributes } from '../../attributes/elements/voice-clone.attributes';
import { simpleConfig } from '../../builders';

export const voiceCloneSerializerConfig = simpleConfig(
  'voice-clone',
  voiceCloneAttributes,
);
