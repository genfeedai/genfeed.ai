import { elementSceneAttributes } from '../../attributes/elements/scene.attributes';
import { simpleConfig } from '../../builders';

export const elementSceneSerializerConfig = simpleConfig(
  'element-scene',
  elementSceneAttributes,
);
