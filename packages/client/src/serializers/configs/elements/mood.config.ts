import { elementMoodAttributes } from '../../attributes/elements/mood.attributes';
import { simpleConfig } from '../../builders';

export const elementMoodSerializerConfig = simpleConfig(
  'element-mood',
  elementMoodAttributes,
);
