import { voiceAttributes } from '../../attributes/elements/voice.attributes';
import { simpleConfig } from '../../builders';

export const voiceSerializerConfig = simpleConfig('voice', voiceAttributes);
