import { elementCameraAttributes } from '../../attributes/elements/camera.attributes';
import { simpleConfig } from '../../builders';

export const elementCameraSerializerConfig = simpleConfig(
  'element-camera',
  elementCameraAttributes,
);
