import { presetAttributes } from '../../attributes/elements/preset.attributes';
import { simpleConfig } from '../../builders';

export const presetSerializerConfig = simpleConfig('preset', presetAttributes);
