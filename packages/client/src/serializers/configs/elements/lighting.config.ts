import { elementLightingAttributes } from '../../attributes/elements/lighting.attributes';
import { simpleConfig } from '../../builders';

export const elementLightingSerializerConfig = simpleConfig(
  'element-lighting',
  elementLightingAttributes,
);
