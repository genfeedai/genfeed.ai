import { avatarAttributes } from '../../attributes/ingredients/avatar.attributes';
import { simpleConfig } from '../../builders';

export const avatarSerializerConfig = simpleConfig('avatar', avatarAttributes);
