import { musicAttributes } from '../../attributes/ingredients/music.attributes';
import { simpleConfig } from '../../builders';

export const musicSerializerConfig = simpleConfig('music', musicAttributes);
