import { adInsightAttributes } from '../../attributes/collections/ad-insight.attributes';
import { simpleConfig } from '../../builders';

export const adInsightSerializerConfig = simpleConfig(
  'ad-insight',
  adInsightAttributes,
);
