import { voteAttributes } from '../../attributes/collections/vote.attributes';
import { simpleConfig } from '../../builders';

export const voteSerializerConfig = simpleConfig('vote', voteAttributes);
