import { promptAttributes } from '../../attributes/collections/prompt.attributes';
import { simpleConfig } from '../../builders';

export const promptSerializerConfig = simpleConfig('prompt', promptAttributes);
