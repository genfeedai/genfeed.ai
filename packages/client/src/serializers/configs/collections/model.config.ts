import { modelAttributes } from '../../attributes/collections/model.attributes';
import { simpleConfig } from '../../builders';

export const modelSerializerConfig = simpleConfig('model', modelAttributes);
