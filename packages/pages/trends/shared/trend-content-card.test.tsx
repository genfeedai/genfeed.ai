import { describe, expect, it } from 'vitest';
import Module from './trend-content-card.tsx';

describe('trend-content-card.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
