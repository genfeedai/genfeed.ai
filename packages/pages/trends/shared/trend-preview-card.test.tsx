import { describe, expect, it } from 'vitest';
import Module from './trend-preview-card.tsx';

describe('trend-preview-card.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
