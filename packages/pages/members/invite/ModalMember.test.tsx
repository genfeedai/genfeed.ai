import { describe, expect, it } from 'vitest';
import Module from './ModalMember.tsx';

describe('ModalMember.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
