import { describe, expect, it } from 'vitest';
import Module from './agent-configuration-page.tsx';

describe('agent-configuration-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
