import { describe, expect, it } from 'vitest';
import Module from './ContentTeamHirePage.tsx';

describe('ContentTeamHirePage.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
