import { describe, expect, it } from 'vitest';
import Module from './CronJobsList.tsx';

describe('CronJobsList.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
