import { describe, expect, it } from 'vitest';
import Module from './ReviewDetailPanel.tsx';

describe('ReviewDetailPanel.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
