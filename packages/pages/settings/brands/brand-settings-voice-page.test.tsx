import { describe, expect, it } from 'vitest';
import Module from './brand-settings-voice-page.tsx';

describe('brand-settings-voice-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
