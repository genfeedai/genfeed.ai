import { describe, expect, it } from 'vitest';
import Module from './brand-settings-publishing-page.tsx';

describe('brand-settings-publishing-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
