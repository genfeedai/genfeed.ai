import { describe, expect, it } from 'vitest';
import Module from './organization-generation-defaults-card.tsx';

describe('organization-generation-defaults-card.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
