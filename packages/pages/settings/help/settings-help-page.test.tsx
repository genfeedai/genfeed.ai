import { describe, expect, it } from 'vitest';
import Module from './settings-help-page.tsx';

describe('settings-help-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
