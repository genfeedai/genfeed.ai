import { describe, expect, it } from 'vitest';
import Module from './streaks-page.tsx';

describe('streaks-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
