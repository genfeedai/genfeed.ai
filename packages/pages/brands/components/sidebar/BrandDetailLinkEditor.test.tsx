import { describe, expect, it } from 'vitest';
import Module from './BrandDetailLinkEditor.tsx';

describe('BrandDetailLinkEditor.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
