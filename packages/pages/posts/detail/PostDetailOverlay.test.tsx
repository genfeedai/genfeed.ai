import { describe, expect, it } from 'vitest';
import Module from './PostDetailOverlay.tsx';

describe('PostDetailOverlay.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
