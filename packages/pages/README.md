# Pages

Cross-app shared page-level React modules and screen compositions.

This directory is reserved for page content reused by more than one app.
Keep app-only pages in the owning app and keep low-level primitives in `packages/ui`.
