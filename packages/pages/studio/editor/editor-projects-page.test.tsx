import { describe, expect, it } from 'vitest';
import Module from './editor-projects-page.tsx';

describe('editor-projects-page.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
