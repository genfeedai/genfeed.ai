import { describe, expect, it } from 'vitest';
import Module from './AgentRunCard.tsx';

describe('AgentRunCard.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
