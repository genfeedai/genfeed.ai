import { describe, expect, it } from 'vitest';
import Module from './RunHistoryList.tsx';

describe('RunHistoryList.tsx', () => {
  it('exports a component', () => {
    expect(Module).toBeDefined();
  });
});
